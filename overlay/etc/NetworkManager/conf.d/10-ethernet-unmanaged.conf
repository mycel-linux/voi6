# Delegate wired ethernet to dhcpcd — NM handles wifi only.
# dhcpcd is faster and more reliable for simple wired DHCP;
# running both on the same interface causes races and duplicate leases.
[device]
match-device=type:ethernet
managed=false

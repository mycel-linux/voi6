# Build artifacts — rootfs, package cache, disk image, kernel/initramfs.
# Everything here is generated by bootstrap.sh / build-image.sh and is large
# (the rootfs + xbps cache alone are ~1 GB). Never commit.
/build/

# Editor / OS noise
*.swp
*~
.DS_Store
